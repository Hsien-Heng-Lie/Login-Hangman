{
  "name": "login-hangman",
  "version": "1.0.0",
  "description": "- local sql server express installed",
  "main": "index.js",
  "scripts": {
    "id-install": "npm install --prefix ./identity-server",
    "id-start": "node identity-server/app.js",
    "server-install": "npm install && npm install --prefix ./server",
    "server-start": "node config.js && node server/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}

